import Link from "next/link";
import { providers } from "@/src/catalog/catalog";
export const dynamic = "force-static";
export default function ProvidersPage() { return <main className="authorityPage"><header className="authorityNav"><Link className="brand" href="/">BestAI<span>Agent</span><b>.in</b></Link><nav><Link href="/models">Models</Link><Link href="/agents">Agents</Link><Link href="/providers">Providers</Link></nav></header><section className="authorityHero"><span className="kicker">Source registry</span><h1>AI Providers</h1><p>These are discovery sources, not endorsements. A provider becomes publishable only after its domain and evidence pass verification.</p></section><section className="authorityGrid">{providers.map(p => <article className="authorityCard" key={p.id}><span className="cardEyebrow">{p.verificationStatus}</span><h2>{p.name}</h2><p>{p.description ?? "Provider description not yet verified."}</p><a href={p.officialUrl} rel="nofollow">Official site →</a><Link href={`/providers/${p.slug}`}>Provider record →</Link></article>)}</section></main>; }
