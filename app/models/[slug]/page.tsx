import { notFound } from "next/navigation";
import Link from "next/link";
import { catalog } from "@/src/catalog/catalog";
import { isEntityIndexable } from "@/src/catalog/verification";
export const dynamicParams = false;
export function generateStaticParams(){ return catalog.filter(isEntityIndexable).map(e => ({slug:e.slug})); }
export default async function ModelPage({params}:{params:Promise<{slug:string}>}) { const {slug}=await params; const m=catalog.find(e=>e.slug===slug && "modelType" in e); if(!m || !isEntityIndexable(m)) notFound(); return <main className="authorityPage"><header className="authorityNav"><Link className="brand" href="/">BestAI<span>Agent</span><b>.in</b></Link></header><section className="authorityHero"><span className="kicker">Verified model</span><h1>{m.name}</h1><p>{m.description ?? "Description not verified."}</p><p>Provider: {m.providerName} · Last verified: {m.lastChecked.slice(0,10)}</p></section><section className="authorityGrid"><article className="authorityCard"><h2>Identity</h2><p>Official model ID: {m.officialModelId ?? "Not verified"}</p><p>Family: {m.family ?? "Not verified"}</p><p>Status: {m.status}</p><p>Modalities: {m.modalities.input.join(", ") || "Unknown"} → {m.modalities.output.join(", ") || "Unknown"}</p></article><article className="authorityCard"><h2>Evidence</h2>{m.evidence.map(e=><p key={e.id}><a href={e.sourceUrl} rel="nofollow">{e.sourceTitle ?? e.sourceUrl}</a> · {e.verificationStatus}</p>)}</article></section></main>; }
